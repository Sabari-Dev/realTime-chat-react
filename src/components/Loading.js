import React from "react";

const Loading = () => {
  return (
    <div className="loading-page">
      <h2 className="load-content">Loading....</h2>
    </div>
  );
};

export default Loading;
